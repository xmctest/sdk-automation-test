# Development overrides. Merged after `.env` when running `npm run gen:env:dev`.
# Copy to `.env.dev` — file is gitignored.

# Enable this together with individual `CSDK_EXPERIMENTAL_*` flags while testing
# experimental Content SDK features locally.
#CSDK_GLOBAL_EXPERIMENTAL_FEATURES_ENABLED=true
